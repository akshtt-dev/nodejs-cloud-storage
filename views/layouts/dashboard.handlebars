<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/src/tailwindcss/output.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/src/js/navbar/script.js"></script>
</head>

<body class="h-screen overflow-hidden">
    <nav
        class="sm:hidden bg-gradient-to-br from-slate-800 to-slate-900 w-full sticky top-0 left-0 py-2 px-2 flex justify-end shadow-md z-10">
        <button onclick="showSlider()">{{{hamburgerSvg}}}</button>
    </nav>

    <main class="flex flex-row h-screen w-full box-border overflow-hidden">
        <section
            class="bg-gradient-to-br from-slate-800 to-slate-900 w-2/6 lg:w-1/6 h-full min-h-screen pt-2 sm:border-r-4 sm:border-gray-400 sm:block hidden overflow-hidden"
            id="navPanel">
            <h1 class="text-slate-200 text-2xl text-center">{{user}}</h1>
            <div class="flex flex-col h-full justify-between overflow-hidden">
                <div class="flex flex-col px-6 gap-4 pt-4 overflow-hidden h-full">
                    <a href="/dashboard">
                        <div class="px-2 py-1 bg-slate-700 rounded-md h-8">
                            <h1 class="text-slate-200 font-semibold text-md">Dashboard</h1>
                        </div>
                    </a>
                    <a href="/dashboard/upload">
                        <div class="px-2 py-1 bg-slate-700 rounded-md h-8">
                            <h1 class="text-slate-200 font-semibold text-md">Upload</h1>
                        </div>
                    </a>
                    <a href="/dashboard/files">
                        <div class="px-2 py-1 bg-slate-700 rounded-md h-8">
                            <h1 class="text-slate-200 font-semibold text-md">Files</h1>
                        </div>
                    </a>
                    <a href="/auth/logout" class="mt-auto md:mb-14 mb-28">
                        <div class="px-2 py-1 bg-slate-700 rounded-md h-8">
                            <h1 class="text-slate-200 font-semibold text-md">Logout</h1>
                        </div>
                    </a>
                </div>
            </div>
        </section>

        <section class="bg-gradient-to-br from-slate-700 to-slate-900 w-full h-full overflow-hidden" id="mainPanel">
            {{{body}}}
        </section>
    </main>
</body>

</html>